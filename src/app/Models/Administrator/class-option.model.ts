export class ClassOption {

    id: number;
    name: string;
    familyId: number;

    constructor(
        id: number,
        name: string,
        familyId: number
    ) {
        this.id = id;
        this.name = name;
        this.familyId = familyId;
    }
}